codecov:
  require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "70...100"

  ignore:
    - "*/tests/*"
    - "*/test_*.py"
    - "*/__pycache__/*"
    - "*/migrations/*"

comment:
  layout: "reach,diff,flags,tree,reach"
  behavior: default
  require_changes: no
